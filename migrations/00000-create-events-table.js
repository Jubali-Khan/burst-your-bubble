exports.up = async function up(sql) {
  console.log('Creating events table...');
  await sql`
    CREATE TABLE events (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      event_title varchar NOT NULL,
      left_logo varchar NOT NULL,
      left_link varchar NOT NULL,
      left_headline varchar NOT NULL,
      left_author_s varchar NOT NULL,
      right_logo varchar NOT NULL,
      right_link varchar NOT NULL,
      right_headline varchar NOT NULL,
      right_author_s varchar NOT NULL,
      event_link varchar NOT NULL
    );
  `;
};

exports.down = async function up(sql) {
  console.log('Dropping events table...');
  await sql`DROP TABLE events;`;
};
