exports.up = async function up(sql) {
  console.log('Creating all_notifications table...');
  await sql`
    CREATE TABLE all_notifications (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      user_id integer REFERENCES comment_reports (user_id) ON DELETE CASCADE,
      comment_id integer REFERENCES comment_reports (id) ON DELETE CASCADE,
      event_id integer REFERENCES comment_reports (event_id) ON DELETE CASCADE,
      reported_for integer REFERENCES comment_reports (reported_for) ON DELETE CASCADE,
      shown boolean NOT NULL
    );
  `;
};

exports.down = async function up(sql) {
  console.log('Dropping all_notifications table...');
  await sql`DROP TABLE all_notifications;`;
};
